steps:
  # Docker 이미지 빌드
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/github-collector:$COMMIT_SHA'
      - '-t'
      - 'gcr.io/$PROJECT_ID/github-collector:latest'
      - '.'
    dir: 'github_collector'

  # Container Registry에 푸시
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/github-collector:$COMMIT_SHA'

  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'push'
      - 'gcr.io/$PROJECT_ID/github-collector:latest'

images:
  - 'gcr.io/$PROJECT_ID/github-collector:$COMMIT_SHA'
  - 'gcr.io/$PROJECT_ID/github-collector:latest'

options:
  logging: CLOUD_LOGGING_ONLY
